# Exclude CodeQL pack compilation cache and resolved dependency directories.
# These are generated by `codeql pack install`, `codeql pack publish`, and `codeql pack bundle`.
.codeql

# Exclude compiled CodeQL query bytecode files.
# These are OS/architecture-specific and must be generated locally by `codeql pack install`.
*.qlx
