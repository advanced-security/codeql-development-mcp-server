# Build artifacts
dist/
server/
coverage/
*.vsix

# Customizations bundle output (generated by scripts/bundle-customizations.js).
# Anchored with a leading slash so the patterns only match the bundle output
# at the extension root, not the tracked source under customizations/.
/agents/
/prompts/
/skills/
/dist-customizations-manifest.json

# @vscode/test-cli downloads and runtime data
.vscode-test/
